import React from 'react';

import { View, Text } from 'react-native';

function StationDescription({ navigate }) {
  return (
    <View style={{ flex: 1, textAlign: 'center' }}>
      <Text>Hello world</Text>
    </View>
  );
}

export default StationDescription;
